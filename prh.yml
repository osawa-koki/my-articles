version: 1
rules:
  - expected: Google
    patterns:
      - /google/i
  - expected: GitHub
    patterns:
      - /github/i
  - expected: Git
    patterns:
      - /git/i
  - expected: AWS
    patterns:
      - /aws/i
  - expected: Google Cloud
    patterns:
      - /google cloud/i
      - /gcp/i
  - expected: Azure
    patterns:
      - /azure/i
  - expected: DynamoDB
    patterns:
      - /dynamo ?db/i
  - expected: Ruby
    patterns:
      - /ruby/i
  - expected: Ruby on Rails
    patterns:
      - /ruby on rails/i
  - expected: GraphQL
    patterns:
      - /graphql/i
  - expected: ユーザー
    patterns:
      - /ユーザ(?!ー)/
  - expected: サーバー
    patterns:
      - /サーバ(?!ー)/
